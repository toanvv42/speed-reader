# Field Notes

The reader should keep ordinary prose readable, even when punctuation is dense: quotes, semicolons, and inline `code` should not break the paragraph.

## Findings

| Signal | Result | Notes |
| --- | --- | --- |
| Headings | OK | chapter picker should work |
| Tables | OK | displayed as full blocks |

```rust
let pace = "technical";
assert_eq!(pace, "technical");
```

![diagram](assets/diagram.png)

Final paragraph with Vietnamese: Đây là một đoạn tiếng Việt để kiểm tra dấu và khoảng trắng.

